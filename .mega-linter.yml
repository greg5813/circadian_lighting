# Configuration file for MegaLinter
#
# See all available variables at https://megalinter.io/latest/config-file/ and in
# linters documentation

# all, none, or list of linter keys
APPLY_FIXES: all

# If you use ENABLE variable, all other languages/formats/tooling-formats will
# be disabled by default
# ENABLE:

# If you use ENABLE_LINTERS variable, all other linters will be disabled by
# default
# ENABLE_LINTERS:

# DISABLE:
# - COPYPASTE # Uncomment to disable checks of excessive copy-pastes
# - SPELL # Uncomment to disable checks of spelling mistakes

SHOW_ELAPSED_TIME: true

# Bandit: skip B101 (assert_used) — standard pytest practice
PYTHON_BANDIT_ARGUMENTS:
  - --skip
  - B101

# Pylint: skip import-error — HA deps not installed in linter env
PYTHON_PYLINT_ARGUMENTS:
  - --disable=import-error

# Mypy: handle HA namespace packages and missing imports
PYTHON_MYPY_ARGUMENTS:
  - --namespace-packages
  - --explicit-package-bases
  - --ignore-missing-imports

# Uncomment if you want MegaLinter to detect errors but not block CI to pass
# DISABLE_ERRORS: true
